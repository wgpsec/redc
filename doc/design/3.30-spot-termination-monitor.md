# 3.30 Spot 实例销毁监控

## 概述

抢占式实例（Spot Instance）会在价格超出出价或资源不足时被云厂商自动回收。本功能通过后台 TCP 连通性探测，监控运行中的 Spot 实例是否被回收，并在回收时通知用户、更新场景状态。

## 架构设计

### 监控方式

**TCP SSH 端口探测（主方案）**

| 项目 | 说明 |
|------|------|
| 探测目标 | `public_ip:22`（SSH 端口） |
| 探测间隔 | 每 120 秒一轮扫描 |
| 重试机制 | 3 次连续尝试，每次 timeout 10s，间隔 15s |
| 判定逻辑 | 3 次全部失败 → 判定为已回收 |
| 去重保护 | 同一 case 只告警一次（`alerted` map） |
| 启动延迟 | 应用启动后 60s 开始首次扫描 |

选择 TCP 探测而非云 API 的原因：
- 不需要额外的云厂商 API 权限
- 跨云通用（阿里云、AWS、火山引擎等一致逻辑）
- 实现简单，无需集成多套 SDK

### 流程图

```
App.startup()
  └─ SpotMonitor.Start()
       └─ goroutine loop (每 120s)
            ├─ LoadProjectCases()
            ├─ 过滤: state == "running" && isSpot == true && !alerted
            ├─ 获取 public_ip (TfOutput)
            └─ probeSSH(ip) — TCP Dial :22 × 3
                 ├─ 成功 → 跳过
                 └─ 失败 → handleTerminated()
                            ├─ case.StatusChange("terminated")
                            ├─ EventsEmit("spot-terminated", {caseId, caseName, template})
                            ├─ emitLog("⚠️ 抢占式实例已被回收: xxx")
                            └─ emitRefresh()

Frontend (Cases.svelte)
  └─ EventsOn("spot-terminated")
       ├─ 显示 Toast 通知（10s 自动消失）
       └─ refresh() 刷新列表
```

## 文件改动

### 新增文件

| 文件 | 说明 |
|------|------|
| `app_spot_monitor.go` | SpotMonitor 结构体 + 后台 goroutine 监控逻辑 |

### 修改文件

| 文件 | 改动 |
|------|------|
| `mod/entity.go` | 新增 `StateTerminated = "terminated"` 状态常量 |
| `app.go` | App struct 新增 `spotMonitor` 字段；startup() 中初始化并启动 |
| `i18n/zh.go` | 新增 `app_spot_monitor_start_success`、`app_spot_terminated` |
| `i18n/en.go` | 同上英文版 |
| `frontend/src/lib/i18n.js` | 新增 `spotTerminated`、`spotTerminatedTitle`、`terminated` |
| `frontend/src/components/Cases/Cases.svelte` | stateConfig 增加 `terminated`；监听 `spot-terminated` 事件；Spot badge 根据状态变红；Toast 通知 UI |
| `frontend/src/components/CustomDeployment/CustomDeploymentList.svelte` | stateConfig 增加 `terminated` |

## 关键代码

### SpotMonitor 结构

```go
type SpotMonitor struct {
    app      *App
    stopCh   chan struct{}
    wg       sync.WaitGroup
    interval time.Duration
    alerted  map[string]bool  // 防重复告警
    mu       sync.Mutex
}
```

### 探测逻辑

```go
func (m *SpotMonitor) probeSSH(ip string) bool {
    addr := net.JoinHostPort(ip, "22")
    for attempt := 0; attempt < 3; attempt++ {
        conn, err := net.DialTimeout("tcp", addr, 10*time.Second)
        if err == nil {
            conn.Close()
            return true  // 可达
        }
        // 间隔 15s 重试
    }
    return false  // 3 次全部失败
}
```

### 扫描过滤条件

仅对满足以下全部条件的 case 进行探测：
1. `state == "running"`
2. `detectSpotFromTfFiles(path) == true`（复用 3.29 的 spot 检测逻辑）
3. 尚未被标记为 alerted

### 前端 Spot Badge 状态变化

| 场景状态 | Badge 样式 |
|---------|-----------|
| running + spot | 琥珀色 `text-amber-700 bg-amber-50` — "抢占式" |
| terminated + spot | 红色闪烁 `text-red-700 bg-red-50 animate-pulse` — "已回收" |

### Toast 通知

- 位置：右下角固定定位
- 样式：红色警告（⚠️ 图标 + 场景名称）
- 持续：10 秒自动消失，可手动关闭
- 触发：收到 `spot-terminated` 事件

## 扩展方向

### 云 API 精确检测（未来）

可通过各云厂商的 DescribeInstances API 精确判断实例状态：

| 云厂商 | API | 回收判据 |
|--------|-----|---------|
| 阿里云 | `DescribeInstances` | `Status != "Running"` |
| AWS | `DescribeInstances` | `State.Name != "running"` |
| 火山引擎 | `DescribeInstances` | `Status != "RUNNING"` |

需从 terraform state 中提取 instance ID，并调用对应 SDK。结构上已预留 `checkSpotCaseViaAPI()` 扩展接口。

## 注意事项

- **误报**：网络波动可能导致误判，但 3 次重试 + 每次 10s timeout 大幅降低概率
- **状态恢复**：用户重新 `start_case` 后会变回 `running`，`ResetAlert()` 可清除告警记录
- **性能**：每轮扫描只检查 running + spot 的 case，通常数量很少，不会造成性能问题

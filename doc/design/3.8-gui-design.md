# GUI 设计规范

## 概述

本文档记录 RedC GUI 的设计规范，包括配色、边框、按钮等样式定义，供后续维护参考。

---

## 1. 页面整体风格

### 1.1 卡片容器

```html
<div class="bg-white rounded-xl border border-gray-100 p-5">
```

| 属性 | 值 |
|------|-----|
| 背景色 | `bg-white` (白色) |
| 圆角 | `rounded-xl` (12px) |
| 边框 | `border border-gray-100` (灰色-100, 1px) |
| 内边距 | `p-5` (20px) 或 `p-4` (16px) |

### 1.2 页面背景

```html
<div class="min-h-screen bg-gray-50">
```

| 属性 | 值 |
|------|-----|
| 最小高度 | `min-h-screen` (100vh) |
| 背景色 | `bg-gray-50` (灰色-50) |

---

## 2. 颜色系统

### 2.1 主色调

| 用途 | 颜色类 | 色值 |
|------|--------|------|
| 主按钮背景 | `bg-blue-600` | #2563EB |
| 主按钮悬停 | `bg-blue-700` | #1D4ED8 |
| 主按钮文字 | `text-white` | #FFFFFF |
| 链接/强调 | `text-blue-600` | #2563EB |
| 链接悬停 | `text-blue-700` | #1D4ED8 |
| 标签背景 | `bg-blue-100` | #DBEAFE |
| 标签文字 | `text-blue-700` | #1D4ED8 |
| 图标背景(主) | `bg-blue-50` | #EFF6FF |
| 图标颜色(主) | `text-blue-600` | #2563EB |

### 2.2 品牌色（Logo）

| 用途 | 颜色类 | 色值 |
|------|--------|------|
| Logo 渐变起始 | `from-rose-500` | #F43F5E |
| Logo 渐变结束 | `to-red-600` | #DC2626 |
| 侧边栏选中强调 | `text-rose-600` | #E11D48 |
| 侧边栏选中背景 | `bg-rose-50` | #FFF1F2 |
| 按钮(危险操作) | `bg-rose-600` | #E11D48 |

### 2.3 状态颜色

#### 成功/运行中

| 用途 | 颜色类 | 色值 |
|------|--------|------|
| 文字 | `text-emerald-600` | #059669 |
| 背景 | `bg-emerald-50` | #ECFDF5 |
| 边框点 | `bg-emerald-500` | #10B981 |

#### 错误/异常

| 用途 | 颜色类 | 色值 |
|------|--------|------|
| 文字 | `text-red-600` | #DC2626 |
| 背景 | `bg-red-50` | #FEF2F2 |
| 边框点 | `bg-red-500` | #EF4444 |

#### 信息/停止

| 用途 | 颜色类 | 色值 |
|------|--------|------|
| 文字 | `text-gray-500` | #6B7280 |
| 背景 | `bg-gray-50` | #F9FAFB |

### 2.3 灰色系

| 用途 | 颜色类 | 色值 |
|------|--------|------|
| 边框（卡片） | `border-gray-100` | #E5E7EB |
| 边框（次要按钮/输入框） | `border-gray-300` | #D1D5DB |
| 边框悬停 | `hover:border-gray-200` | #E5E7EB |
| 按钮背景 | `bg-white` | #FFFFFF |
| 按钮悬停 | `hover:bg-gray-50` | #F9FAFB |
| 输入框背景 | `bg-gray-50` | #F9FAFB |
| 次要文字 | `text-gray-500` | #6B7280 |
| 禁用文字 | `text-gray-400` | #9CA3AF |
| 主要文字 | `text-gray-900` | #111827 |

---

## 3. 按钮规范

### 3.1 主要按钮（Primary）

```html
<button class="h-10 px-5 bg-blue-600 text-white text-[13px] font-medium rounded-lg hover:bg-blue-700 transition-colors">
```

| 属性 | 值 |
|------|-----|
| 高度 | `h-10` (40px) |
| 水平内边距 | `px-5` (20px) |
| 背景色 | `bg-blue-600` |
| 文字颜色 | `text-white` |
| 文字大小 | `text-[13px]` |
| 字体粗细 | `font-medium` (500) |
| 圆角 | `rounded-lg` (8px) |
| 悬停背景 | `hover:bg-blue-700` |
| 过渡动画 | `transition-colors` |

### 3.2 次要按钮（Secondary）

```html
<button class="h-10 px-5 text-gray-700 bg-white border border-gray-300 hover:bg-gray-50 text-[13px] font-medium rounded-lg transition-colors">
```

| 属性 | 值 |
|------|-----|
| 高度 | `h-10` (40px) |
| 水平内边距 | `px-5` (20px) |
| 背景色 | `bg-white` |
| 边框 | `border border-gray-300` |
| 文字颜色 | `text-gray-700` |
| 文字大小 | `text-[13px]` |
| 字体粗细 | `font-medium` |
| 圆角 | `rounded-lg` |
| 悬停背景 | `hover:bg-gray-50` |

### 3.3 小型按钮

```html
<button class="h-7 px-2.5 text-[10px] font-medium rounded-lg">
```

| 属性 | 值 |
|------|-----|
| 高度 | `h-7` (28px) |
| 水平内边距 | `px-2.5` (10px) |
| 文字大小 | `text-[10px]` |

### 3.4 禁用状态

```html
<button class="disabled:opacity-50 disabled:cursor-not-allowed">
```

| 属性 | 值 |
|------|-----|
| 透明度 | `disabled:opacity-50` (50%) |
| 光标 | `disabled:cursor-not-allowed` |

---

## 4. 输入框规范

### 4.1 文本输入框（无边框）

```html
<input class="w-full h-10 px-3 text-[13px] bg-gray-50 border-0 rounded-lg text-gray-900 placeholder-gray-400 focus:ring-2 focus:ring-gray-900 focus:ring-offset-1 transition-shadow">
```

| 属性 | 值 |
|------|-----|
| 宽度 | `w-full` (100%) |
| 高度 | `h-10` (40px) |
| 内边距 | `px-3` (12px) |
| 背景色 | `bg-gray-50` |
| 边框 | `border-0` (无) |
| 圆角 | `rounded-lg` |
| 文字大小 | `text-[13px]` |
| 文字颜色 | `text-gray-900` |
| 占位符颜色 | `placeholder-gray-400` |
| 聚焦边框 | `focus:ring-2 focus:ring-gray-900 focus:ring-offset-1` |
| 过渡阴影 | `transition-shadow` |

### 4.2 文本输入框（带边框）

```html
<input class="w-full px-3 py-2 text-[13px] border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-gray-900">
```

| 属性 | 值 |
|------|-----|
| 边框 | `border border-gray-300` |
| 聚焦边框 | `focus:ring-2 focus:ring-gray-900` |

### 4.3 下拉选择框

```html
<select class="h-10 px-3 text-[13px] bg-gray-50 border-0 rounded-lg text-gray-900 focus:ring-2 focus:ring-gray-900">
```

---

## 5. 字体大小规范

| 用途 | 类名 | 大小 |
|------|------|------|
| 页面标题 | `text-lg` | 18px |
| 卡片标题 | `text-[15px]` | 15px |
| 正文/按钮 | `text-[13px]` | 13px |
| 辅助文字 | `text-[12px]` | 12px |
| 标签/小字 | `text-[11px]` | 11px |
| 超小文字 | `text-[10px]` | 10px |

---

## 6. 状态标签

### 6.1 状态标签（Status Badge）

```html
<span class="inline-flex items-center gap-1.5 px-2.5 py-1 text-[11px] font-medium rounded-full bg-emerald-50 text-emerald-600">
```

| 属性 | 值 |
|------|-----|
| 布局 | `inline-flex items-center gap-1.5` |
| 内边距 | `px-2.5 py-1` |
| 文字大小 | `text-[11px]` |
| 字体粗细 | `font-medium` |
| 圆角 | `rounded-full` |
| 背景 | `bg-emerald-50` |
| 文字 | `text-emerald-600` |

### 6.2 版本标签

```html
<span class="px-2 py-0.5 bg-blue-100 text-blue-700 text-xs font-medium rounded">
```

| 属性 | 值 |
|------|-----|
| 内边距 | `px-2 py-0.5` |
| 背景 | `bg-blue-100` |
| 文字 | `text-blue-700` |
| 文字大小 | `text-xs` (12px) |

---

## 7. 组件示例

### 7.1 设置项卡片

```html
<div class="bg-white rounded-xl border border-gray-100 p-4 sm:p-5">
  <div class="flex flex-col sm:flex-row sm:items-center justify-between gap-3 sm:gap-0">
    <div>
      <div class="text-[13px] sm:text-[14px] font-medium text-gray-900">{title}</div>
      <div class="text-[11px] sm:text-[12px] text-gray-500 mt-1">{description}</div>
    </div>
    <button class="...">开关按钮</button>
  </div>
</div>
```

### 7.2 空状态

```html
<div class="px-5 py-8 text-center text-[13px] text-gray-400">
  {emptyMessage}
</div>
```

### 7.3 加载状态

```html
<div class="text-[13px] text-gray-500">
  {loadingMessage || '加载中...'}
</div>
```

---

## 8. 响应式断点

| 断点 | 前缀 | 宽度 |
|------|------|------|
| 手机 | `sm:` | ≥640px |
| 平板 | `md:` | ≥768px |
| 桌面 | `lg:` | ≥1024px |

**常用响应式写法：**

```html
<!-- 高度响应式 -->
<input class="h-9 sm:h-10 ...">

<!-- 文字大小响应式 -->
<div class="text-[12px] sm:text-[13px] ...">

<!-- 内边距响应式 -->
<button class="px-4 sm:px-5 ...">
```

---

## 9. 图标规范

### 9.1 小图标（工具栏）

```html
<svg class="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
```

| 属性 | 值 |
|------|-----|
| 尺寸 | `w-4 h-4` (16px) |
| 填充 | `fill-none` |
| 描边宽度 | `stroke-width="2"` |

### 9.2 中图标（卡片内）

```html
<div class="w-10 h-10 rounded-lg bg-blue-50 flex items-center justify-center">
  <svg class="w-5 h-5 text-blue-600" ...>
</div>
```

| 属性 | 值 |
|------|-----|
| 容器尺寸 | `w-10 h-10` (40px) |
| 容器圆角 | `rounded-lg` |
| 容器背景 | `bg-blue-50` |
| 图标尺寸 | `w-5 h-5` (20px) |
| 图标颜色 | `text-blue-600` |

---

## 10. 关键原则

1. **一致性优先**: 所有按钮高度统一使用 `h-10`，圆角统一使用 `rounded-lg`
2. **色彩语义**: 蓝色用于主要操作，绿色用于成功状态，红色用于错误状态
3. **层级分明**: 页面背景 → 卡片背景 → 输入框背景，依次加深
4. **响应式设计**: 移动端优先，小屏幕用较小尺寸，大屏幕自动适配
5. **可访问性**: 确保文字与背景有足够对比度，使用 `aria-label` 等属性
